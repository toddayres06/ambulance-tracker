# Backend .env file

# Database connection
DB_USER=ambulance_tracker_clean_user
DB_PASSWORD=sgHT23UTY3MWfDyq8TsJIkVVpsy8Epic
DB_HOST=dpg-d0st94ali9vc73dcbnjg-a.virginia-postgres.render.com
DB_PORT=5432
DB_NAME=ambulance_tracker_clean
DB_SSL=true  # Set to true since you are using SSL

# Rebuild DATABASE_URL for Prisma to use
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require"

# JWT secret for authentication
JWT_SECRET=my_ambulance_company

# Optional (If needed for TLS issues)
# NODE_TLS_REJECT_UNAUTHORIZED=0  # Uncomment this only if you're facing SSL/TLS issues and you understand the security risks
